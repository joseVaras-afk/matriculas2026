# --- DATASOURCE ---
spring.datasource.url=jdbc:mysql://localhost:3306/matriculas2026?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Santiago&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=Sistem12
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# --- LOGGING (opcional) ---
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# --- CORS (lo afinamos más adelante si necesitas) ---
app.cors.allowed-origins=http://localhost:5173,http://localhost:4200,http://localhost:3000

# Evita que Hibernate convierta a snake_case los nombres de columnas/tablas
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.enabled=true
# === MAIL (Gmail con TLS) ===
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=matriculasarturoalessandri@gmail.com
spring.mail.password=mdvr nnkx loqk sudy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8

# Remitente por defecto (lo usamos si no llega desde otro lado)
app.mail.from=Matriculas 2026 <tu_cuenta@gmail.com>
# DEBUG de envío de correo y de Thymeleaf
logging.level.org.springframework.mail=DEBUG
logging.level.org.thymeleaf=INFO
logging.level.com.sun.mail=DEBUG
logging.level.jakarta.mail=DEBUG
mail.debug=true

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.freemarker.FreeMarkerAutoConfiguration

spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false


# Opcional: si quieres que falle al iniciar si SMTP no conecta
# spring.mail.test-connection=true